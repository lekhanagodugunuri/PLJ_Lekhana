'use strict';


const first_name = require("./people.json");

//get array length
console.log(people.length);

// task 1
const substring = 'rob';
const isRobMap = first_name.map(first_name =>first_name.includes(substring));

console.log(isRobMap);
console.log(first_name.includes(substring))

function findFirstname(item){

if(first_name.includes(substring)){
    
}

}
console.log(findFirstname)